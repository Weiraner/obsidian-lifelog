# Screenshots / assets

Drop screenshots here, then they'll show up in the top-level README:

- `calendar.png` — the time-block calendar (open `demo-vault/Dashboard.md` in Reading view, screenshot the week)
- `expenses.png` — the expense dashboard (概览 tab)

Optional: a short GIF of the quick-capture hotkey → parse → dashboard flow makes
a strong hero image.
